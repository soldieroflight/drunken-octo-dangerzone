import pygameclass DisplayLayer(object):    def __init__(self):        passclass DisplayManager(object):    def __init__(self):        self.layers = {}            def addLayer(self, layerName):        if string in self.layers.keys():            print("[DisplayManager] Layer %s is already in use!")            return false                    else:            self.layers[layerName] = DisplayLayer()